meta {
  name: Lock stake submit
  type: http
  seq: 30
}

post {
  url: {{baseUrl}}/chain/stake/lock-submit
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "tx": "{{signed_lock_tx}}"
  }
}
